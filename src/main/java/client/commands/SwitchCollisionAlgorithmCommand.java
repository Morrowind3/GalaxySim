package client.commands;

import javafx.event.Event;
import javafx.event.EventHandler;

public class SwitchCollisionAlgorithmCommand implements EventHandler<Event> {
    @Override
    public void handle(Event event) {

    }
}
